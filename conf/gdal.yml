apiVersion: v1
kind: Pod
metadata:
  name: gdal
  labels:
    run: gdal
spec:
  containers:
  - image: valluzzi/gdal:latest
    name: gdal
    command: 
      - gdalinfo
    args:
      - --version
  dnsPolicy: ClusterFirst
  restartPolicy: Never
